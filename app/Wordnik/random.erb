<div id="home">
  <div class="toolbar">
    <h2><%= @definitions[0]['headword'] %></h2>
  </div>
  <ul class="rounded">
  	<% @definitions.each do |definition| %>
	  <li>
	    (<%= definition['partOfSpeech'] %>)
	    <%= definition['text'] %>
	  </li>
  	<% end %>
</div>
